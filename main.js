 function calcularDivisores (numero){
    const divisores =[];
    for (let i = 2; i<numero;i++) {
        if (numero%i===0){
            divisores.push(i);
        } else {
            continue;
        }
    }
    return divisores;
};
console.log(calcularDivisores(200));

function calcularNumerosPrimos(divisores){
    divisores = divisores;
    for (let i =divisores.length;i>0;i--) {
        if
    }
}
